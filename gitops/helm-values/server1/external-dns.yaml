# ExternalDNS — server1 cluster overrides
# Shared base values: gitops/helm-values/external-dns.yaml

# Only manage records under server1.home
domainFilters:
  - server1.home
  # This one should be moved to server 1
  - qr.home

# TXT registry owner ID — must be unique per cluster to avoid record conflicts
txtOwnerId: homelab-server1
