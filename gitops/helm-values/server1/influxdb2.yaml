# InfluxDB2 — server1 cluster overrides
# Shared base values: gitops/helm-values/influxdb2.yaml
# No cluster-specific Helm overrides needed; hostname is set in gitops/k8s-manifests/server1/influxdb2/HTTPRoute.yaml
