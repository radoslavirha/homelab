# Headlamp —  cluster overrides
# Shared base values: gitops/helm-values/headlamp.yaml

httpRoute:
  # Both names for the migration; the .home entry comes out at stage 7.
  hostnames:
    - headlamp.server1.home
    - headlamp.server1.homelab.irha.cz
