# MongoDB —  cluster overrides
# Shared base values: gitops/helm-values/mongodb.yaml
# No cluster-specific Helm overrides needed; TCP route is set in gitops/k8s-manifests//mongodb/IngressRouteTCP.yaml
