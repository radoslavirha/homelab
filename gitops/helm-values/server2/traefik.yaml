# Traefik — server2 cluster overrides
# Shared base values: gitops/helm-values/traefik.yaml

ingressRoute:
  dashboard:
    enabled: true
    matchRule: "Host(`traefik.server2.homelab.irha.cz`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))"
    # Forward-auth. Everything this route serves — the dashboard AND /api, which lists every
    # router, service and middleware in the cluster — now goes past Authentik first. The
    # Middleware is k8s-manifests/server2/traefik/Middleware.authentik.yaml, in this same
    # namespace; the login callback for this host is its own HTTPRoute next to it, because
    # matchRule above covers only /dashboard and /api.
    middlewares:
      - name: authentik
        namespace: traefik
    # websecure only. The one hostname this route answers to is covered by the cluster
    # certificate, so there is nothing left for `web` to serve — the .home name it existed
    # for was retired at stage 7.
    entryPoints: [websecure]
    tls:
      # An IngressRoute is served by the kubernetesCRD provider, which does not see
      # the Gateway listener's certificateRefs. Without this Traefik answers with its
      # built-in self-signed certificate and the browser warns.
      secretName: server2-tls
    annotations:
      external-dns.alpha.kubernetes.io/target: "192.168.1.201"


# HTTPS Gateway listener. Per cluster because certificateRefs names a cluster-specific
# Secret. See gitops/helm-values/server3/traefik.yaml for the full reasoning — including
# why there is deliberately no 80 -> 443 redirect yet (stage 7.5, after OTLP moves).
gateway:
  listeners:
    websecure:
      port: 443
      protocol: HTTPS
      namespacePolicy:
        from: All
      certificateRefs:
        - name: server2-tls

service:
  # type: ClusterIP is in the shared values file; only the address is per cluster.
  spec:
    externalIPs:
      - 192.168.1.201

providers:
  kubernetesGateway:
    statusAddress:
      ip: "192.168.1.201"

# No TCP/UDP entrypoints. server1's Traefik opens 1883/8883/27017 and UDP 4000/4001 for the
# IoT estate; server2's entire IoT estate was removed on 2026-09-13, so every one of those
# entrypoints routed to nothing. The IngressRouteTCP objects that backed them went with it.
# If server2 ever needs a TCP/UDP entrypoint again, copy the block from
# gitops/helm-values/server1/traefik.yaml and read the hostNetwork note there first.
#
# ── Removing the chart's own `traefik` entrypoint ────────────────────────────────────
# Under hostNetwork it binds :8080 on the node IP. It served NOTHING — `api.insecure: false`
# in the shared values keeps the API and dashboard off it, and /, /dashboard/,
# /api/http/routers and /api/overview all answered 404 on all three nodes (measured
# 2026-09-16). An open socket that serves nothing is still worth removing.
#
# ALL THREE KEYS MOVE TOGETHER or the pod dies: both probes default to `ports.traefik.port`
# and Traefik serves /ping on that entrypoint (chart 41.5.0). So the entrypoint goes, ping
# moves to `web`, and healthchecksPort follows it to 80. Rendered against all three clusters
# before landing: server1 keeps mongodb/mqtt/mqtts and both UDP entrypoints, server3 keeps
# otlp-grpc, and every dashboard IngressRoute already runs on websecure.
#
# server2 only, for now: updateStrategy is Recreate, so this bounces the cluster's ingress
# for a few seconds, and the shared values file would have bounced all three at once. Once
# server1 and server3 follow, these three keys belong in gitops/helm-values/traefik.yaml.
#
# The side effect: /ping is now answered on :80 for any Host. It returns "OK" and nothing
# else, and it is what the kubelet probes.
ports:
  traefik: null

deployment:
  healthchecksPort: 80

additionalArguments:
  - "--ping.entryPoint=web"
