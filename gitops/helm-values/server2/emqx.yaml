# EMQX — server2 cluster overrides
# Shared base values: gitops/helm-values/emqx.yaml
# Hostname is set in gitops/k8s-manifests/server2/emqx/HTTPRoute.yaml

emqxConfig:
  EMQX_opentelemetry__traces__e2e_tracing_options:
    cluster_identifier: emqx-server2
