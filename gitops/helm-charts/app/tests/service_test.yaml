suite: service tests
templates:
  - templates/service.yaml
release:
  name: my-app
tests:
  - it: should create a Service for each enabled service entry
    set:
      image.repository: ghcr.io/example/my-app
      labels.component: api
      labels.partOf: iot
      ingress.enabled: false
      services.http.enabled: true
      services.http.protocol: TCP
      services.http.port: 80
      services.http.targetPort: 4000
    asserts:
      - isKind:
          of: Service
      - equal:
          path: metadata.name
          value: api-iot-my-app-http

  - it: should use short service name as port name
    set:
      image.repository: ghcr.io/example/my-app
      labels.component: api
      labels.partOf: iot
      ingress.enabled: false
      services.http.enabled: true
      services.http.port: 80
      services.http.targetPort: 4000
    asserts:
      - equal:
          path: spec.ports[0].name
          value: http

  - it: should NOT create a Service when disabled
    set:
      image.repository: ghcr.io/example/my-app
      labels.component: api
      labels.partOf: iot
      ingress.enabled: false
      services.http.enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should be type ClusterIP
    set:
      image.repository: ghcr.io/example/my-app
      labels.component: api
      labels.partOf: iot
      ingress.enabled: false
      services.http.enabled: true
      services.http.port: 80
      services.http.targetPort: 4000
    asserts:
      - equal:
          path: spec.type
          value: ClusterIP

  - it: should include standard Helm labels
    set:
      image.repository: ghcr.io/example/my-app
      labels.component: api
      labels.partOf: iot
      ingress.enabled: false
      services.http.enabled: true
      services.http.port: 80
      services.http.targetPort: 4000
    asserts:
      - isNotEmpty:
          path: metadata.labels["helm.sh/chart"]
      - equal:
          path: metadata.labels["app.kubernetes.io/managed-by"]
          value: Helm
