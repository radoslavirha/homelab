# qr-manager-ui — ApplicationSet (server1 × production + sandbox)
# Chart: gitops/helm-charts/app/ (path-based, no publishing)
# No ExternalSecrets (no secretRefs), no cluster-specific overrides.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: qr-manager-ui
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - list:
              elements:
                - cluster: server1
                  clusterServer: https://192.168.1.200:6443
          - list:
              elements:
                - env: production
                - env: sandbox
  template:
    metadata:
      name: qr-manager-ui-{{cluster}}-{{env}}
    spec:
      project: default
      sources:
        - repoURL: https://github.com/radoslavirha/homelab
          targetRevision: HEAD
          path: gitops/helm-charts/app
          helm:
            releaseName: qr-manager-ui
            valueFiles:
              - $values/gitops/helm-values/server1/apps/qr-manager-ui/values.yaml
              - $values/gitops/helm-values/server1/apps/qr-manager-ui/values-{{env}}.yaml
              # Every template variable (vars:) is in a file, so whoever edits a config can find it.
              # Loaded last: an app file cannot shadow a variable by accident.
              - $values/gitops/helm-values/server1/apps/vars/common.yaml
              - $values/gitops/helm-values/server1/apps/vars/{{env}}.yaml
        - repoURL: https://github.com/radoslavirha/homelab
          targetRevision: HEAD
          ref: values
      destination:
        server: '{{clusterServer}}'
        namespace: '{{env}}'
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          selfHeal: true
          prune: true
